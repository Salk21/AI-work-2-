# Задание 1.3.1

import numpy as np

m = np.zeros((8,8)) # задаём матрицу размер 8х8
m [::2, 1::2] = 1 # 1 столбец: срез 1 значения по счёту на второй шаг ставим 1 (повторяем с шагом два)
m [1::2, ::2] = 1 # 2 столбец: на первом шаге ставим 1 и делаем срез на втором шаге (повторяем с шагом два)
print(m)
